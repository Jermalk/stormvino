# SCRATCHPAD.md — in-session working memory

> Cleared at start of every session. Carry-over summary written as first entry.
> Format: bullet points, max 5 lines per topic, no prose.

## Carried over:
Session 42: Fixed `_tier_map_for_provider` sentinel bug (None/rank-0 → all 176 tests pass). SVP Phase 5: responsive polish (header hides .host/.clock ≤640px, VramBar legend hides ≤640px, grid already 1-col ≤900px). GPU bars gem: replaced GpuPanel 2×2 specs-grid with BarMeter rows for GT temp/VRAM temp/Fan/Power; added optional `label` prop to BarMeter. Charts race fix: loadSeq counter discards stale auto-refresh responses. infergate gap analysis written to plans/20260514_infergate_gaps.md — 8 gaps, P0 bug is prefer-loaded tier restriction.
